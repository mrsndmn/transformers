container:
  image: "cr.ai.cloud.ru/aicloud-base-images/cuda12.1-torch2-py310:0.0.36"
  command: /workspace-SR004.nfs2/d.tarasov/envs/adaptive_attention/bin/python -c 'import torch; print( torch.cuda.is_available() ) '
  work_dir: '.'
    #   run_from_copy: True # Рекомендуется запускать задачи именно из копий, чтобы избежать неожиданного поведения при запуске задач
    #   cry_copy_dir: "/home/jovyan/.cryri"

cloud:
  region: "SR004"
  instance_type: "a100.2gpu"
  n_workers: 1
